import { useNavigate } from "react-router-dom";
import characters from '../data/characters.json';

export default function CharacterSelect() {
    const navigate = useNavigate();

    const ChooseCharacter = (char) => {
        setPlayer
    }